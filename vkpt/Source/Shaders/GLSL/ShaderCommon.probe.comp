#version 460

#extension GL_EXT_nonuniform_qualifier : require
#extension GL_EXT_samplerless_texture_functions : require

// Generated by GenerateShaderCommonProbe.py, do not edit by hand. The probe has no shader stage
// of its own: it exists so that CheckShaderProperties.py can compile both generated headers and
// compare the layouts, the bindings and the framebuffer declarations that glslc and dxc derive
// from them. Every framebuffer is touched, because dxc drops the unused declarations and the
// checker can only compare the ones inside a function.

#define DESC_SET_FRAMEBUFFERS 0
#define DESC_SET_GLOBAL_UNIFORM 1

// The same two headers, in the same order, as ShaderCommonGLSLFunc.h includes them.
#include "Utils.h"
#include "ShaderCommonGLSL.h"

#define PROBE_DESC_SET 2

// The global uniform is declared by ShaderCommonGLSLFunc.h, so the probe declares it in the very
// same shape.
layout(
    set = DESC_SET_GLOBAL_UNIFORM,
    binding = BINDING_GLOBAL_UNIFORM)
    readonly uniform GlobalUniform_BT
{
    ShGlobalUniform globalUniform;
};

layout(push_constant) uniform Push_BT
{
    ShVertPreprocessing push;
};

layout(set = PROBE_DESC_SET, binding = 0, std430) readonly buffer ProbeShVertex_BT
{
    ShVertex vertices[];
};

layout(set = PROBE_DESC_SET, binding = 1, std430) readonly buffer ProbeShGeometryInstance_BT
{
    ShGeometryInstance geometryInstances[];
};

layout(set = PROBE_DESC_SET, binding = 2, std430) readonly buffer ProbeShTonemapping_BT
{
    ShTonemapping tonemapping[];
};

layout(set = PROBE_DESC_SET, binding = 3, std430) readonly buffer ProbeShLightEncoded_BT
{
    ShLightEncoded lights[];
};

layout(set = PROBE_DESC_SET, binding = 4, std430) readonly buffer ProbeShIndirectDrawCommand_BT
{
    ShIndirectDrawCommand drawCmds[];
};

layout(set = PROBE_DESC_SET, binding = 5, std430) readonly buffer ProbeShLensFlareInstance_BT
{
    ShLensFlareInstance lensFlares[];
};

layout(set = PROBE_DESC_SET, binding = 6, std430) readonly buffer ProbeShDecalInstance_BT
{
    ShDecalInstance decals[];
};

layout(set = PROBE_DESC_SET, binding = 7, std140) uniform readonly ProbeShPortalInstance_BT
{
    ShPortalInstance portals[PORTAL_MAX_COUNT];
};

layout(set = PROBE_DESC_SET, binding = 8, std430) writeonly buffer ProbeOutput_BT
{
    float probeOutput[];
};

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

void main()
{
    float v = 0.0;

    ivec2 pix = ivec2(0);
    vec2 uv = vec2(0.5);

    // ShGlobalUniform
    v += globalUniform.view[0][0] + globalUniform.invView[0][0] + globalUniform.viewPrev[0][0] +
         globalUniform.projection[0][0] + globalUniform.invProjection[0][0] +
         globalUniform.projectionPrev[0][0] + globalUniform.volumeViewProj[0][0] +
         globalUniform.volumeViewProjInv[0][0] + globalUniform.volumeViewProj_Prev[0][0] +
         globalUniform.volumeViewProjInv_Prev[0][0] + globalUniform.cellWorldSize +
         globalUniform.renderWidth + float(globalUniform.__pad0) + float(globalUniform.__pad1) +
         globalUniform.renderHeight + float(globalUniform.frameId) + globalUniform.timeDelta +
         globalUniform.minLogLuminance + globalUniform.maxLogLuminance + globalUniform.luminanceWhitePoint +
         float(globalUniform.stopEyeAdaptation) + float(globalUniform.directionalLightExists) +
         globalUniform.polyLightSpotlightFactor + float(globalUniform.skyType) +
         globalUniform.skyColorMultiplier + float(globalUniform.skyCubemapIndex) +
         globalUniform.skyColorDefault.x + globalUniform.cameraPosition.x +
         globalUniform.cameraPositionPrev.x + float(globalUniform.debugShowFlags) +
         float(globalUniform.indirSecondBounce) + float(globalUniform.lightCount) +
         float(globalUniform.lightCountPrev) + globalUniform.emissionMapBoost +
         globalUniform.emissionMaxScreenColor + globalUniform.normalMapStrength +
         globalUniform.skyColorSaturation + globalUniform.emissionSharpMask +
         globalUniform.talSelfLitOffset + float(globalUniform.emissionBlendMode) +
         globalUniform.emissionBlendStrength + globalUniform.skyAmbientLod + globalUniform.rayLength +
         float(globalUniform.rayCullBackFaces) + float(globalUniform.rayCullMaskWorld) +
         globalUniform.bloomIntensity + globalUniform.bloomThreshold +
         globalUniform.bloomEmissionMultiplier + float(globalUniform.reflectRefractMaxDepth) +
         float(globalUniform.cameraMediaType) + globalUniform.indexOfRefractionWater +
         globalUniform.indexOfRefractionGlass + globalUniform.waterTextureDerivativesMultiplier +
         float(globalUniform.volumeEnableType) + globalUniform.volumeScattering +
         float(globalUniform.forceNoWaterRefraction) + float(globalUniform.waterNormalTextureIndex) +
         float(globalUniform.noBackfaceReflForNoMediaChange) + globalUniform.time +
         globalUniform.waterWaveSpeed + globalUniform.waterWaveStrength +
         globalUniform.waterColorAndDensity.x + globalUniform.acidColorAndDensity.x +
         globalUniform.cameraRayConeSpreadAngle + globalUniform.waterTextureAreaScale +
         float(globalUniform.squareInputRoughness) + globalUniform.upscaledRenderWidth +
         globalUniform.worldUpVector.x + globalUniform.upscaledRenderHeight + globalUniform.jitterX +
         globalUniform.jitterY + globalUniform.primaryRayMinDist +
         float(globalUniform.rayCullMaskWorld_Shadow) + float(globalUniform.lensFlareCullingInputCount) +
         float(globalUniform.applyViewProjToLensFlares) + float(globalUniform.twirlPortalNormal) +
         float(globalUniform.lightIndexIgnoreFPVShadows) + globalUniform.gradientMultDiffuse +
         globalUniform.gradientMultIndirect + globalUniform.gradientMultSpecular +
         globalUniform.minRoughness + globalUniform.volumeCameraNear + globalUniform.volumeCameraFar +
         float(globalUniform.antiFireflyEnabled) + globalUniform.volumeAmbient.x +
         globalUniform.volumeSourceColor.x + globalUniform.volumeDirToSource.x +
         globalUniform.volumeSourceAsymmetry + float(globalUniform.coreQ2RTX) +
         float(globalUniform.q2DepthGradMode) + globalUniform.skyNee +
         float(globalUniform.q2LightStatsMode) + float(globalUniform.reflRefrEarlyOut) +
         float(globalUniform.neeLightSamples) + globalUniform.turbWarpStrength +
         float(globalUniform.instanceGeomInfoOffset[0].x) +
         float(globalUniform.instanceGeomInfoOffsetPrev[0].x) + float(globalUniform.instanceGeomCount[0].x) +
         globalUniform.viewProjCubemap[0][0][0] + globalUniform.skyCubemapRotationTransform[0][0] +
         globalUniform.fogMins[0].x + float(globalUniform.fogIsActive[0]) + globalUniform.fogMaxs[0].x +
         globalUniform.fogColor[0].x + globalUniform.fogDensity[0].x + globalUniform.lightStyleScales[0].x +
         globalUniform.giBounceRays.x + globalUniform.fltEnable.x + globalUniform.fixedAlbedo.x +
         globalUniform.sunBounce.x + globalUniform.levelFogColorDensity.x + globalUniform.levelFogSkyBlend.x;

    // ShVertPreprocessing
    v += float(push.tlasInstanceCount) + float(push.tlasInstanceIsDynamicBits[0]);

    // ShVertex
    v += vertices[0].position.x + vertices[0].normal.x + vertices[0].texCoord.x +
         vertices[0].texCoordLayer1.x + vertices[0].texCoordLayer2.x + float(vertices[0].packedColor) +
         float(vertices[0].cluster) + float(vertices[0].lightStyles) + float(vertices[0].__pad0) +
         float(vertices[0].__pad1) + float(vertices[0].__pad2);

    // ShGeometryInstance
    v += geometryInstances[0].model[0][0] + geometryInstances[0].prevModel[0][0] +
         geometryInstances[0].materialColors[0].x + float(geometryInstances[0].materials0A) +
         float(geometryInstances[0].materials0B) + float(geometryInstances[0].materials0C) +
         float(geometryInstances[0].materials1A) + float(geometryInstances[0].materials1B) +
         float(geometryInstances[0].portalIndex) + float(geometryInstances[0].materials2A) +
         float(geometryInstances[0].materials2B) + float(geometryInstances[0]._unused0) +
         float(geometryInstances[0].flags) + float(geometryInstances[0].baseVertexIndex) +
         float(geometryInstances[0].baseIndexIndex) + float(geometryInstances[0].prevBaseVertexIndex) +
         float(geometryInstances[0].prevBaseIndexIndex) + float(geometryInstances[0].vertexCount) +
         float(geometryInstances[0].indexCount) + geometryInstances[0].defaultRoughness +
         geometryInstances[0].defaultMetallicity + geometryInstances[0].defaultEmission +
         float(geometryInstances[0]._unused1);

    // ShTonemapping
    v += tonemapping[0].tmExposureBias + tonemapping[0].tmExposureSpeedDown +
         tonemapping[0].tmExposureSpeedUp + tonemapping[0].tmLowPercentile +
         tonemapping[0].tmHighPercentile + tonemapping[0].tmMinLuminance + tonemapping[0].tmMaxLuminance +
         tonemapping[0].tmNoiseBlend + tonemapping[0].tmNoiseStops + tonemapping[0].tmDynRangeStops +
         tonemapping[0].tmReinhard + tonemapping[0].tmKneeStart + tonemapping[0].tmWhitePoint +
         tonemapping[0].tmSlopeBlurSigma + tonemapping[0].frameTime + float(tonemapping[0].resetCurve) +
         tonemapping[0].kneeW + tonemapping[0].kneeA + tonemapping[0].kneeB +
         float(tonemapping[0].histogram[0]) + tonemapping[0].curve[0] + tonemapping[0].normalized[0] +
         tonemapping[0].adaptedLuminance + tonemapping[0].avgLuminance;

    // ShLightEncoded
    v += lights[0].color.x + float(lights[0].lightType) + lights[0].data_0.x + lights[0].data_1.x +
         lights[0].data_2.x + lights[0].data_3.x + lights[0].data_4.x + lights[0].data_5.x +
         lights[0].data_6.x + lights[0].data_7.x;

    // ShIndirectDrawCommand
    v += float(drawCmds[0].indexCount) + float(drawCmds[0].instanceCount) + float(drawCmds[0].firstIndex) +
         float(drawCmds[0].vertexOffset) + float(drawCmds[0].firstInstance) + drawCmds[0].positionToCheck_X +
         drawCmds[0].positionToCheck_Y + drawCmds[0].positionToCheck_Z;

    // ShLensFlareInstance
    v += float(lensFlares[0].textureIndex);

    // ShDecalInstance
    v += decals[0].transform[0][0] + float(decals[0].textureAlbedoAlpha) +
         float(decals[0].textureRougnessMetallic) + float(decals[0].textureNormals) + float(decals[0].__pad0);

    // ShPortalInstance
    v += portals[0].inPosition.x + portals[0].outPosition.x + portals[0].outDirection.x + portals[0].outUp.x;

    // One slot per framebuffer: its storage image, its sampled view and its sampler.
    // The sampler is paired with the sampled view of the very same slot, which is the
    // pairing the split table is for, and with a sampler that is used by nothing else.

    // framebufAlbedo  r11f_g11f_b10f
    imageStore(framebufAlbedo, pix, vec4(1.0));
    v += float(imageLoad(framebufAlbedo, pix).x);
    v += float(texelFetch(framebufAlbedo_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufAlbedo_Sampled, framebufAlbedo_Sampler), uv).x);

    // framebufAlbedo_Prev  r11f_g11f_b10f
    imageStore(framebufAlbedo_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufAlbedo_Prev, pix).x);
    v += float(texelFetch(framebufAlbedo_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufAlbedo_Prev_Sampled, framebufAlbedo_Prev_Sampler), uv).x);

    // framebufIsSky  r8ui
    imageStore(framebufIsSky, pix, uvec4(1u));
    v += float(imageLoad(framebufIsSky, pix).x);
    v += float(texelFetch(framebufIsSky_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufIsSky_Sampled, framebufIsSky_Sampler), uv).x);

    // framebufNormal  r32ui
    imageStore(framebufNormal, pix, uvec4(1u));
    v += float(imageLoad(framebufNormal, pix).x);
    v += float(texelFetch(framebufNormal_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufNormal_Sampled, framebufNormal_Sampler), uv).x);

    // framebufNormal_Prev  r32ui
    imageStore(framebufNormal_Prev, pix, uvec4(1u));
    v += float(imageLoad(framebufNormal_Prev, pix).x);
    v += float(texelFetch(framebufNormal_Prev_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufNormal_Prev_Sampled, framebufNormal_Prev_Sampler), uv).x);

    // framebufNormalGeometry  r32ui
    imageStore(framebufNormalGeometry, pix, uvec4(1u));
    v += float(imageLoad(framebufNormalGeometry, pix).x);
    v += float(texelFetch(framebufNormalGeometry_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufNormalGeometry_Sampled, framebufNormalGeometry_Sampler), uv).x);

    // framebufNormalGeometry_Prev  r32ui
    imageStore(framebufNormalGeometry_Prev, pix, uvec4(1u));
    v += float(imageLoad(framebufNormalGeometry_Prev, pix).x);
    v += float(texelFetch(framebufNormalGeometry_Prev_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufNormalGeometry_Prev_Sampled, framebufNormalGeometry_Prev_Sampler), uv).x);

    // framebufMetallicRoughness  rg8
    imageStore(framebufMetallicRoughness, pix, vec4(1.0));
    v += float(imageLoad(framebufMetallicRoughness, pix).x);
    v += float(texelFetch(framebufMetallicRoughness_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufMetallicRoughness_Sampled, framebufMetallicRoughness_Sampler), uv).x);

    // framebufMetallicRoughness_Prev  rg8
    imageStore(framebufMetallicRoughness_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufMetallicRoughness_Prev, pix).x);
    v += float(texelFetch(framebufMetallicRoughness_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufMetallicRoughness_Prev_Sampled, framebufMetallicRoughness_Prev_Sampler), uv).x);

    // framebufDepthWorld  r16f
    imageStore(framebufDepthWorld, pix, vec4(1.0));
    v += float(imageLoad(framebufDepthWorld, pix).x);
    v += float(texelFetch(framebufDepthWorld_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufDepthWorld_Sampled, framebufDepthWorld_Sampler), uv).x);

    // framebufDepthWorld_Prev  r16f
    imageStore(framebufDepthWorld_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufDepthWorld_Prev, pix).x);
    v += float(texelFetch(framebufDepthWorld_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufDepthWorld_Prev_Sampled, framebufDepthWorld_Prev_Sampler), uv).x);

    // framebufDepthGrad  r16f
    imageStore(framebufDepthGrad, pix, vec4(1.0));
    v += float(imageLoad(framebufDepthGrad, pix).x);
    v += float(texelFetch(framebufDepthGrad_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufDepthGrad_Sampled, framebufDepthGrad_Sampler), uv).x);

    // framebufDepthNdc  r32f
    imageStore(framebufDepthNdc, pix, vec4(1.0));
    v += float(imageLoad(framebufDepthNdc, pix).x);
    v += float(texelFetch(framebufDepthNdc_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufDepthNdc_Sampled, framebufDepthNdc_Sampler), uv).x);

    // framebufMotion  rgba16f
    imageStore(framebufMotion, pix, vec4(1.0));
    v += float(imageLoad(framebufMotion, pix).x);
    v += float(texelFetch(framebufMotion_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufMotion_Sampled, framebufMotion_Sampler), uv).x);

    // framebufUnfilteredDirect  r32ui
    imageStore(framebufUnfilteredDirect, pix, uvec4(1u));
    v += float(imageLoad(framebufUnfilteredDirect, pix).x);
    v += float(texelFetch(framebufUnfilteredDirect_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufUnfilteredDirect_Sampled, framebufUnfilteredDirect_Sampler), uv).x);

    // framebufUnfilteredSpecular  r32ui
    imageStore(framebufUnfilteredSpecular, pix, uvec4(1u));
    v += float(imageLoad(framebufUnfilteredSpecular, pix).x);
    v += float(texelFetch(framebufUnfilteredSpecular_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufUnfilteredSpecular_Sampled, framebufUnfilteredSpecular_Sampler), uv).x);

    // framebufUnfilteredIndirectSH_R  rgba16f
    imageStore(framebufUnfilteredIndirectSH_R, pix, vec4(1.0));
    v += float(imageLoad(framebufUnfilteredIndirectSH_R, pix).x);
    v += float(texelFetch(framebufUnfilteredIndirectSH_R_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufUnfilteredIndirectSH_R_Sampled, framebufUnfilteredIndirectSH_R_Sampler), uv).x);

    // framebufUnfilteredIndirectSH_G  rgba16f
    imageStore(framebufUnfilteredIndirectSH_G, pix, vec4(1.0));
    v += float(imageLoad(framebufUnfilteredIndirectSH_G, pix).x);
    v += float(texelFetch(framebufUnfilteredIndirectSH_G_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufUnfilteredIndirectSH_G_Sampled, framebufUnfilteredIndirectSH_G_Sampler), uv).x);

    // framebufUnfilteredIndirectSH_B  rgba16f
    imageStore(framebufUnfilteredIndirectSH_B, pix, vec4(1.0));
    v += float(imageLoad(framebufUnfilteredIndirectSH_B, pix).x);
    v += float(texelFetch(framebufUnfilteredIndirectSH_B_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufUnfilteredIndirectSH_B_Sampled, framebufUnfilteredIndirectSH_B_Sampler), uv).x);

    // framebufSurfacePosition  rgba32f
    imageStore(framebufSurfacePosition, pix, vec4(1.0));
    v += float(imageLoad(framebufSurfacePosition, pix).x);
    v += float(texelFetch(framebufSurfacePosition_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufSurfacePosition_Sampled, framebufSurfacePosition_Sampler), uv).x);

    // framebufSurfacePosition_Prev  rgba32f
    imageStore(framebufSurfacePosition_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufSurfacePosition_Prev, pix).x);
    v += float(texelFetch(framebufSurfacePosition_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufSurfacePosition_Prev_Sampled, framebufSurfacePosition_Prev_Sampler), uv).x);

    // framebufVisibilityBuffer  rgba32f
    imageStore(framebufVisibilityBuffer, pix, vec4(1.0));
    v += float(imageLoad(framebufVisibilityBuffer, pix).x);
    v += float(texelFetch(framebufVisibilityBuffer_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufVisibilityBuffer_Sampled, framebufVisibilityBuffer_Sampler), uv).x);

    // framebufVisibilityBuffer_Prev  rgba32f
    imageStore(framebufVisibilityBuffer_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufVisibilityBuffer_Prev, pix).x);
    v += float(texelFetch(framebufVisibilityBuffer_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufVisibilityBuffer_Prev_Sampled, framebufVisibilityBuffer_Prev_Sampler), uv).x);

    // framebufViewDirection  rgba16f
    imageStore(framebufViewDirection, pix, vec4(1.0));
    v += float(imageLoad(framebufViewDirection, pix).x);
    v += float(texelFetch(framebufViewDirection_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufViewDirection_Sampled, framebufViewDirection_Sampler), uv).x);

    // framebufViewDirection_Prev  rgba16f
    imageStore(framebufViewDirection_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufViewDirection_Prev, pix).x);
    v += float(texelFetch(framebufViewDirection_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufViewDirection_Prev_Sampled, framebufViewDirection_Prev_Sampler), uv).x);

    // framebufPrimaryToReflRefr  rgba32ui
    imageStore(framebufPrimaryToReflRefr, pix, uvec4(1u));
    v += float(imageLoad(framebufPrimaryToReflRefr, pix).x);
    v += float(texelFetch(framebufPrimaryToReflRefr_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufPrimaryToReflRefr_Sampled, framebufPrimaryToReflRefr_Sampler), uv).x);

    // framebufThroughput  rgba16f
    imageStore(framebufThroughput, pix, vec4(1.0));
    v += float(imageLoad(framebufThroughput, pix).x);
    v += float(texelFetch(framebufThroughput_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufThroughput_Sampled, framebufThroughput_Sampler), uv).x);

    // framebufPreFinal  r11f_g11f_b10f
    imageStore(framebufPreFinal, pix, vec4(1.0));
    v += float(imageLoad(framebufPreFinal, pix).x);
    v += float(texelFetch(framebufPreFinal_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufPreFinal_Sampled, framebufPreFinal_Sampler), uv).x);

    // framebufFinal  r11f_g11f_b10f
    imageStore(framebufFinal, pix, vec4(1.0));
    v += float(imageLoad(framebufFinal, pix).x);
    v += float(texelFetch(framebufFinal_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufFinal_Sampled, framebufFinal_Sampler), uv).x);

    // framebufUpscaledPing  r11f_g11f_b10f
    imageStore(framebufUpscaledPing, pix, vec4(1.0));
    v += float(imageLoad(framebufUpscaledPing, pix).x);
    v += float(texelFetch(framebufUpscaledPing_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufUpscaledPing_Sampled, framebufUpscaledPing_Sampler), uv).x);

    // framebufUpscaledPong  r11f_g11f_b10f
    imageStore(framebufUpscaledPong, pix, vec4(1.0));
    v += float(imageLoad(framebufUpscaledPong, pix).x);
    v += float(texelFetch(framebufUpscaledPong_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufUpscaledPong_Sampled, framebufUpscaledPong_Sampler), uv).x);

    // framebufMotionDlss  rg16f
    imageStore(framebufMotionDlss, pix, vec4(1.0));
    v += float(imageLoad(framebufMotionDlss, pix).x);
    v += float(texelFetch(framebufMotionDlss_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufMotionDlss_Sampled, framebufMotionDlss_Sampler), uv).x);

    // framebufAccumHistoryLength  rgba16f
    imageStore(framebufAccumHistoryLength, pix, vec4(1.0));
    v += float(imageLoad(framebufAccumHistoryLength, pix).x);
    v += float(texelFetch(framebufAccumHistoryLength_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufAccumHistoryLength_Sampled, framebufAccumHistoryLength_Sampler), uv).x);

    // framebufAccumHistoryLength_Prev  rgba16f
    imageStore(framebufAccumHistoryLength_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufAccumHistoryLength_Prev, pix).x);
    v += float(texelFetch(framebufAccumHistoryLength_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufAccumHistoryLength_Prev_Sampled, framebufAccumHistoryLength_Prev_Sampler), uv).x);

    // framebufDiffTemporary  r32ui
    imageStore(framebufDiffTemporary, pix, uvec4(1u));
    v += float(imageLoad(framebufDiffTemporary, pix).x);
    v += float(texelFetch(framebufDiffTemporary_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufDiffTemporary_Sampled, framebufDiffTemporary_Sampler), uv).x);

    // framebufDiffAccumColor  r32ui
    imageStore(framebufDiffAccumColor, pix, uvec4(1u));
    v += float(imageLoad(framebufDiffAccumColor, pix).x);
    v += float(texelFetch(framebufDiffAccumColor_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufDiffAccumColor_Sampled, framebufDiffAccumColor_Sampler), uv).x);

    // framebufDiffAccumColor_Prev  r32ui
    imageStore(framebufDiffAccumColor_Prev, pix, uvec4(1u));
    v += float(imageLoad(framebufDiffAccumColor_Prev, pix).x);
    v += float(texelFetch(framebufDiffAccumColor_Prev_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufDiffAccumColor_Prev_Sampled, framebufDiffAccumColor_Prev_Sampler), uv).x);

    // framebufDiffAccumMoments  rg16f
    imageStore(framebufDiffAccumMoments, pix, vec4(1.0));
    v += float(imageLoad(framebufDiffAccumMoments, pix).x);
    v += float(texelFetch(framebufDiffAccumMoments_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufDiffAccumMoments_Sampled, framebufDiffAccumMoments_Sampler), uv).x);

    // framebufDiffAccumMoments_Prev  rg16f
    imageStore(framebufDiffAccumMoments_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufDiffAccumMoments_Prev, pix).x);
    v += float(texelFetch(framebufDiffAccumMoments_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufDiffAccumMoments_Prev_Sampled, framebufDiffAccumMoments_Prev_Sampler), uv).x);

    // framebufDiffColorHistory  rgba16f
    imageStore(framebufDiffColorHistory, pix, vec4(1.0));
    v += float(imageLoad(framebufDiffColorHistory, pix).x);
    v += float(texelFetch(framebufDiffColorHistory_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufDiffColorHistory_Sampled, framebufDiffColorHistory_Sampler), uv).x);

    // framebufDiffPingColorAndVariance  rgba16f
    imageStore(framebufDiffPingColorAndVariance, pix, vec4(1.0));
    v += float(imageLoad(framebufDiffPingColorAndVariance, pix).x);
    v += float(texelFetch(framebufDiffPingColorAndVariance_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufDiffPingColorAndVariance_Sampled, framebufDiffPingColorAndVariance_Sampler), uv).x);

    // framebufDiffPongColorAndVariance  rgba16f
    imageStore(framebufDiffPongColorAndVariance, pix, vec4(1.0));
    v += float(imageLoad(framebufDiffPongColorAndVariance, pix).x);
    v += float(texelFetch(framebufDiffPongColorAndVariance_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufDiffPongColorAndVariance_Sampled, framebufDiffPongColorAndVariance_Sampler), uv).x);

    // framebufSpecAccumColor  r32ui
    imageStore(framebufSpecAccumColor, pix, uvec4(1u));
    v += float(imageLoad(framebufSpecAccumColor, pix).x);
    v += float(texelFetch(framebufSpecAccumColor_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufSpecAccumColor_Sampled, framebufSpecAccumColor_Sampler), uv).x);

    // framebufSpecAccumColor_Prev  r32ui
    imageStore(framebufSpecAccumColor_Prev, pix, uvec4(1u));
    v += float(imageLoad(framebufSpecAccumColor_Prev, pix).x);
    v += float(texelFetch(framebufSpecAccumColor_Prev_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufSpecAccumColor_Prev_Sampled, framebufSpecAccumColor_Prev_Sampler), uv).x);

    // framebufSpecPingColor  r32ui
    imageStore(framebufSpecPingColor, pix, uvec4(1u));
    v += float(imageLoad(framebufSpecPingColor, pix).x);
    v += float(texelFetch(framebufSpecPingColor_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufSpecPingColor_Sampled, framebufSpecPingColor_Sampler), uv).x);

    // framebufSpecPongColor  r32ui
    imageStore(framebufSpecPongColor, pix, uvec4(1u));
    v += float(imageLoad(framebufSpecPongColor, pix).x);
    v += float(texelFetch(framebufSpecPongColor_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufSpecPongColor_Sampled, framebufSpecPongColor_Sampler), uv).x);

    // framebufIndirAccumSH_R  rgba16f
    imageStore(framebufIndirAccumSH_R, pix, vec4(1.0));
    v += float(imageLoad(framebufIndirAccumSH_R, pix).x);
    v += float(texelFetch(framebufIndirAccumSH_R_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufIndirAccumSH_R_Sampled, framebufIndirAccumSH_R_Sampler), uv).x);

    // framebufIndirAccumSH_R_Prev  rgba16f
    imageStore(framebufIndirAccumSH_R_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufIndirAccumSH_R_Prev, pix).x);
    v += float(texelFetch(framebufIndirAccumSH_R_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufIndirAccumSH_R_Prev_Sampled, framebufIndirAccumSH_R_Prev_Sampler), uv).x);

    // framebufIndirAccumSH_G  rgba16f
    imageStore(framebufIndirAccumSH_G, pix, vec4(1.0));
    v += float(imageLoad(framebufIndirAccumSH_G, pix).x);
    v += float(texelFetch(framebufIndirAccumSH_G_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufIndirAccumSH_G_Sampled, framebufIndirAccumSH_G_Sampler), uv).x);

    // framebufIndirAccumSH_G_Prev  rgba16f
    imageStore(framebufIndirAccumSH_G_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufIndirAccumSH_G_Prev, pix).x);
    v += float(texelFetch(framebufIndirAccumSH_G_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufIndirAccumSH_G_Prev_Sampled, framebufIndirAccumSH_G_Prev_Sampler), uv).x);

    // framebufIndirAccumSH_B  rgba16f
    imageStore(framebufIndirAccumSH_B, pix, vec4(1.0));
    v += float(imageLoad(framebufIndirAccumSH_B, pix).x);
    v += float(texelFetch(framebufIndirAccumSH_B_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufIndirAccumSH_B_Sampled, framebufIndirAccumSH_B_Sampler), uv).x);

    // framebufIndirAccumSH_B_Prev  rgba16f
    imageStore(framebufIndirAccumSH_B_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufIndirAccumSH_B_Prev, pix).x);
    v += float(texelFetch(framebufIndirAccumSH_B_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufIndirAccumSH_B_Prev_Sampled, framebufIndirAccumSH_B_Prev_Sampler), uv).x);

    // framebufIndirPingSH_R  rgba16f
    imageStore(framebufIndirPingSH_R, pix, vec4(1.0));
    v += float(imageLoad(framebufIndirPingSH_R, pix).x);
    v += float(texelFetch(framebufIndirPingSH_R_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufIndirPingSH_R_Sampled, framebufIndirPingSH_R_Sampler), uv).x);

    // framebufIndirPingSH_G  rgba16f
    imageStore(framebufIndirPingSH_G, pix, vec4(1.0));
    v += float(imageLoad(framebufIndirPingSH_G, pix).x);
    v += float(texelFetch(framebufIndirPingSH_G_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufIndirPingSH_G_Sampled, framebufIndirPingSH_G_Sampler), uv).x);

    // framebufIndirPingSH_B  rgba16f
    imageStore(framebufIndirPingSH_B, pix, vec4(1.0));
    v += float(imageLoad(framebufIndirPingSH_B, pix).x);
    v += float(texelFetch(framebufIndirPingSH_B_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufIndirPingSH_B_Sampled, framebufIndirPingSH_B_Sampler), uv).x);

    // framebufIndirPongSH_R  rgba16f
    imageStore(framebufIndirPongSH_R, pix, vec4(1.0));
    v += float(imageLoad(framebufIndirPongSH_R, pix).x);
    v += float(texelFetch(framebufIndirPongSH_R_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufIndirPongSH_R_Sampled, framebufIndirPongSH_R_Sampler), uv).x);

    // framebufIndirPongSH_G  rgba16f
    imageStore(framebufIndirPongSH_G, pix, vec4(1.0));
    v += float(imageLoad(framebufIndirPongSH_G, pix).x);
    v += float(texelFetch(framebufIndirPongSH_G_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufIndirPongSH_G_Sampled, framebufIndirPongSH_G_Sampler), uv).x);

    // framebufIndirPongSH_B  rgba16f
    imageStore(framebufIndirPongSH_B, pix, vec4(1.0));
    v += float(imageLoad(framebufIndirPongSH_B, pix).x);
    v += float(texelFetch(framebufIndirPongSH_B_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufIndirPongSH_B_Sampled, framebufIndirPongSH_B_Sampler), uv).x);

    // framebufAtrousFilteredVariance  r16f
    imageStore(framebufAtrousFilteredVariance, pix, vec4(1.0));
    v += float(imageLoad(framebufAtrousFilteredVariance, pix).x);
    v += float(texelFetch(framebufAtrousFilteredVariance_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufAtrousFilteredVariance_Sampled, framebufAtrousFilteredVariance_Sampler), uv).x);

    // framebufAcidFogRT  r11f_g11f_b10f
    imageStore(framebufAcidFogRT, pix, vec4(1.0));
    v += float(imageLoad(framebufAcidFogRT, pix).x);
    v += float(texelFetch(framebufAcidFogRT_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufAcidFogRT_Sampled, framebufAcidFogRT_Sampler), uv).x);

    // framebufAcidFog  r11f_g11f_b10f
    imageStore(framebufAcidFog, pix, vec4(1.0));
    v += float(imageLoad(framebufAcidFog, pix).x);
    v += float(texelFetch(framebufAcidFog_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufAcidFog_Sampled, framebufAcidFog_Sampler), uv).x);

    // framebufScreenEmisRT  r11f_g11f_b10f
    imageStore(framebufScreenEmisRT, pix, vec4(1.0));
    v += float(imageLoad(framebufScreenEmisRT, pix).x);
    v += float(texelFetch(framebufScreenEmisRT_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufScreenEmisRT_Sampled, framebufScreenEmisRT_Sampler), uv).x);

    // framebufScreenEmission  r11f_g11f_b10f
    imageStore(framebufScreenEmission, pix, vec4(1.0));
    v += float(imageLoad(framebufScreenEmission, pix).x);
    v += float(texelFetch(framebufScreenEmission_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufScreenEmission_Sampled, framebufScreenEmission_Sampler), uv).x);

    // framebufGodRays  r11f_g11f_b10f
    imageStore(framebufGodRays, pix, vec4(1.0));
    v += float(imageLoad(framebufGodRays, pix).x);
    v += float(texelFetch(framebufGodRays_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufGodRays_Sampled, framebufGodRays_Sampler), uv).x);

    // framebufGodRaysFiltered  r11f_g11f_b10f
    imageStore(framebufGodRaysFiltered, pix, vec4(1.0));
    v += float(imageLoad(framebufGodRaysFiltered, pix).x);
    v += float(texelFetch(framebufGodRaysFiltered_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufGodRaysFiltered_Sampled, framebufGodRaysFiltered_Sampler), uv).x);

    // framebufBloomInput  r11f_g11f_b10f
    imageStore(framebufBloomInput, pix, vec4(1.0));
    v += float(imageLoad(framebufBloomInput, pix).x);
    v += float(texelFetch(framebufBloomInput_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufBloomInput_Sampled, framebufBloomInput_Sampler), uv).x);

    // framebufBloom_Mip1  r11f_g11f_b10f
    imageStore(framebufBloom_Mip1, pix, vec4(1.0));
    v += float(imageLoad(framebufBloom_Mip1, pix).x);
    v += float(texelFetch(framebufBloom_Mip1_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufBloom_Mip1_Sampled, framebufBloom_Mip1_Sampler), uv).x);

    // framebufBloom_Mip2  r11f_g11f_b10f
    imageStore(framebufBloom_Mip2, pix, vec4(1.0));
    v += float(imageLoad(framebufBloom_Mip2, pix).x);
    v += float(texelFetch(framebufBloom_Mip2_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufBloom_Mip2_Sampled, framebufBloom_Mip2_Sampler), uv).x);

    // framebufBloom_Mip3  r11f_g11f_b10f
    imageStore(framebufBloom_Mip3, pix, vec4(1.0));
    v += float(imageLoad(framebufBloom_Mip3, pix).x);
    v += float(texelFetch(framebufBloom_Mip3_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufBloom_Mip3_Sampled, framebufBloom_Mip3_Sampler), uv).x);

    // framebufBloom_Mip4  r11f_g11f_b10f
    imageStore(framebufBloom_Mip4, pix, vec4(1.0));
    v += float(imageLoad(framebufBloom_Mip4, pix).x);
    v += float(texelFetch(framebufBloom_Mip4_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufBloom_Mip4_Sampled, framebufBloom_Mip4_Sampler), uv).x);

    // framebufBloom_Mip5  r11f_g11f_b10f
    imageStore(framebufBloom_Mip5, pix, vec4(1.0));
    v += float(imageLoad(framebufBloom_Mip5, pix).x);
    v += float(texelFetch(framebufBloom_Mip5_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufBloom_Mip5_Sampled, framebufBloom_Mip5_Sampler), uv).x);

    // framebufBloom_Result  r11f_g11f_b10f
    imageStore(framebufBloom_Result, pix, vec4(1.0));
    v += float(imageLoad(framebufBloom_Result, pix).x);
    v += float(texelFetch(framebufBloom_Result_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufBloom_Result_Sampled, framebufBloom_Result_Sampler), uv).x);

    // framebufWipeEffectSource  r11f_g11f_b10f
    imageStore(framebufWipeEffectSource, pix, vec4(1.0));
    v += float(imageLoad(framebufWipeEffectSource, pix).x);
    v += float(texelFetch(framebufWipeEffectSource_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufWipeEffectSource_Sampled, framebufWipeEffectSource_Sampler), uv).x);

    // framebufQ2ColorLF_SH  rgba16f
    imageStore(framebufQ2ColorLF_SH, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2ColorLF_SH, pix).x);
    v += float(texelFetch(framebufQ2ColorLF_SH_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2ColorLF_SH_Sampled, framebufQ2ColorLF_SH_Sampler), uv).x);

    // framebufQ2ColorLF_SH_Prev  rgba16f
    imageStore(framebufQ2ColorLF_SH_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2ColorLF_SH_Prev, pix).x);
    v += float(texelFetch(framebufQ2ColorLF_SH_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2ColorLF_SH_Prev_Sampled, framebufQ2ColorLF_SH_Prev_Sampler), uv).x);

    // framebufQ2ColorLF_COCG  rg16f
    imageStore(framebufQ2ColorLF_COCG, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2ColorLF_COCG, pix).x);
    v += float(texelFetch(framebufQ2ColorLF_COCG_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2ColorLF_COCG_Sampled, framebufQ2ColorLF_COCG_Sampler), uv).x);

    // framebufQ2ColorLF_COCG_Prev  rg16f
    imageStore(framebufQ2ColorLF_COCG_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2ColorLF_COCG_Prev, pix).x);
    v += float(texelFetch(framebufQ2ColorLF_COCG_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2ColorLF_COCG_Prev_Sampled, framebufQ2ColorLF_COCG_Prev_Sampler), uv).x);

    // framebufQ2ColorHF  r32ui
    imageStore(framebufQ2ColorHF, pix, uvec4(1u));
    v += float(imageLoad(framebufQ2ColorHF, pix).x);
    v += float(texelFetch(framebufQ2ColorHF_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufQ2ColorHF_Sampled, framebufQ2ColorHF_Sampler), uv).x);

    // framebufQ2ColorHF_Prev  r32ui
    imageStore(framebufQ2ColorHF_Prev, pix, uvec4(1u));
    v += float(imageLoad(framebufQ2ColorHF_Prev, pix).x);
    v += float(texelFetch(framebufQ2ColorHF_Prev_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufQ2ColorHF_Prev_Sampled, framebufQ2ColorHF_Prev_Sampler), uv).x);

    // framebufQ2ColorSpec  r32ui
    imageStore(framebufQ2ColorSpec, pix, uvec4(1u));
    v += float(imageLoad(framebufQ2ColorSpec, pix).x);
    v += float(texelFetch(framebufQ2ColorSpec_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufQ2ColorSpec_Sampled, framebufQ2ColorSpec_Sampler), uv).x);

    // framebufQ2ColorSpec_Prev  r32ui
    imageStore(framebufQ2ColorSpec_Prev, pix, uvec4(1u));
    v += float(imageLoad(framebufQ2ColorSpec_Prev, pix).x);
    v += float(texelFetch(framebufQ2ColorSpec_Prev_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufQ2ColorSpec_Prev_Sampled, framebufQ2ColorSpec_Prev_Sampler), uv).x);

    // framebufQ2ViewDepth  r32f
    imageStore(framebufQ2ViewDepth, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2ViewDepth, pix).x);
    v += float(texelFetch(framebufQ2ViewDepth_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2ViewDepth_Sampled, framebufQ2ViewDepth_Sampler), uv).x);

    // framebufQ2ViewDepth_Prev  r32f
    imageStore(framebufQ2ViewDepth_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2ViewDepth_Prev, pix).x);
    v += float(texelFetch(framebufQ2ViewDepth_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2ViewDepth_Prev_Sampled, framebufQ2ViewDepth_Prev_Sampler), uv).x);

    // framebufQ2BaseColor  rgba16f
    imageStore(framebufQ2BaseColor, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2BaseColor, pix).x);
    v += float(texelFetch(framebufQ2BaseColor_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2BaseColor_Sampled, framebufQ2BaseColor_Sampler), uv).x);

    // framebufQ2BaseColor_Prev  rgba16f
    imageStore(framebufQ2BaseColor_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2BaseColor_Prev, pix).x);
    v += float(texelFetch(framebufQ2BaseColor_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2BaseColor_Prev_Sampled, framebufQ2BaseColor_Prev_Sampler), uv).x);

    // framebufQ2Metallic  rg8
    imageStore(framebufQ2Metallic, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2Metallic, pix).x);
    v += float(texelFetch(framebufQ2Metallic_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2Metallic_Sampled, framebufQ2Metallic_Sampler), uv).x);

    // framebufQ2Metallic_Prev  rg8
    imageStore(framebufQ2Metallic_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2Metallic_Prev, pix).x);
    v += float(texelFetch(framebufQ2Metallic_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2Metallic_Prev_Sampled, framebufQ2Metallic_Prev_Sampler), uv).x);

    // framebufQ2BounceThroughput  rgba16f
    imageStore(framebufQ2BounceThroughput, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2BounceThroughput, pix).x);
    v += float(texelFetch(framebufQ2BounceThroughput_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2BounceThroughput_Sampled, framebufQ2BounceThroughput_Sampler), uv).x);

    // framebufQ2Transparent  rgba16f
    imageStore(framebufQ2Transparent, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2Transparent, pix).x);
    v += float(texelFetch(framebufQ2Transparent_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2Transparent_Sampled, framebufQ2Transparent_Sampler), uv).x);

    // framebufQ2GodRaysThroughputDist  rgba16f
    imageStore(framebufQ2GodRaysThroughputDist, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2GodRaysThroughputDist, pix).x);
    v += float(texelFetch(framebufQ2GodRaysThroughputDist_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2GodRaysThroughputDist_Sampled, framebufQ2GodRaysThroughputDist_Sampler), uv).x);

    // framebufQ2FogAccum  rgba16f
    imageStore(framebufQ2FogAccum, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2FogAccum, pix).x);
    v += float(texelFetch(framebufQ2FogAccum_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2FogAccum_Sampled, framebufQ2FogAccum_Sampler), uv).x);

    // framebufQ2HistColorLF_SH  rgba16f
    imageStore(framebufQ2HistColorLF_SH, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2HistColorLF_SH, pix).x);
    v += float(texelFetch(framebufQ2HistColorLF_SH_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2HistColorLF_SH_Sampled, framebufQ2HistColorLF_SH_Sampler), uv).x);

    // framebufQ2HistColorLF_SH_Prev  rgba16f
    imageStore(framebufQ2HistColorLF_SH_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2HistColorLF_SH_Prev, pix).x);
    v += float(texelFetch(framebufQ2HistColorLF_SH_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2HistColorLF_SH_Prev_Sampled, framebufQ2HistColorLF_SH_Prev_Sampler), uv).x);

    // framebufQ2HistColorLF_COCG  rg16f
    imageStore(framebufQ2HistColorLF_COCG, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2HistColorLF_COCG, pix).x);
    v += float(texelFetch(framebufQ2HistColorLF_COCG_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2HistColorLF_COCG_Sampled, framebufQ2HistColorLF_COCG_Sampler), uv).x);

    // framebufQ2HistColorLF_COCG_Prev  rg16f
    imageStore(framebufQ2HistColorLF_COCG_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2HistColorLF_COCG_Prev, pix).x);
    v += float(texelFetch(framebufQ2HistColorLF_COCG_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2HistColorLF_COCG_Prev_Sampled, framebufQ2HistColorLF_COCG_Prev_Sampler), uv).x);

    // framebufQ2HistColorHF  r32ui
    imageStore(framebufQ2HistColorHF, pix, uvec4(1u));
    v += float(imageLoad(framebufQ2HistColorHF, pix).x);
    v += float(texelFetch(framebufQ2HistColorHF_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufQ2HistColorHF_Sampled, framebufQ2HistColorHF_Sampler), uv).x);

    // framebufQ2HistColorHF_Prev  r32ui
    imageStore(framebufQ2HistColorHF_Prev, pix, uvec4(1u));
    v += float(imageLoad(framebufQ2HistColorHF_Prev, pix).x);
    v += float(texelFetch(framebufQ2HistColorHF_Prev_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufQ2HistColorHF_Prev_Sampled, framebufQ2HistColorHF_Prev_Sampler), uv).x);

    // framebufQ2HistMomentsHF  rgba16f
    imageStore(framebufQ2HistMomentsHF, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2HistMomentsHF, pix).x);
    v += float(texelFetch(framebufQ2HistMomentsHF_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2HistMomentsHF_Sampled, framebufQ2HistMomentsHF_Sampler), uv).x);

    // framebufQ2HistMomentsHF_Prev  rgba16f
    imageStore(framebufQ2HistMomentsHF_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2HistMomentsHF_Prev, pix).x);
    v += float(texelFetch(framebufQ2HistMomentsHF_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2HistMomentsHF_Prev_Sampled, framebufQ2HistMomentsHF_Prev_Sampler), uv).x);

    // framebufQ2FilteredSpec  rgba16f
    imageStore(framebufQ2FilteredSpec, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2FilteredSpec, pix).x);
    v += float(texelFetch(framebufQ2FilteredSpec_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2FilteredSpec_Sampled, framebufQ2FilteredSpec_Sampler), uv).x);

    // framebufQ2FilteredSpec_Prev  rgba16f
    imageStore(framebufQ2FilteredSpec_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2FilteredSpec_Prev, pix).x);
    v += float(texelFetch(framebufQ2FilteredSpec_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2FilteredSpec_Prev_Sampled, framebufQ2FilteredSpec_Prev_Sampler), uv).x);

    // framebufQ2AtrousPingLF_SH  rgba16f
    imageStore(framebufQ2AtrousPingLF_SH, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2AtrousPingLF_SH, pix).x);
    v += float(texelFetch(framebufQ2AtrousPingLF_SH_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2AtrousPingLF_SH_Sampled, framebufQ2AtrousPingLF_SH_Sampler), uv).x);

    // framebufQ2AtrousPongLF_SH  rgba16f
    imageStore(framebufQ2AtrousPongLF_SH, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2AtrousPongLF_SH, pix).x);
    v += float(texelFetch(framebufQ2AtrousPongLF_SH_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2AtrousPongLF_SH_Sampled, framebufQ2AtrousPongLF_SH_Sampler), uv).x);

    // framebufQ2AtrousPingLF_COCG  rg16f
    imageStore(framebufQ2AtrousPingLF_COCG, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2AtrousPingLF_COCG, pix).x);
    v += float(texelFetch(framebufQ2AtrousPingLF_COCG_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2AtrousPingLF_COCG_Sampled, framebufQ2AtrousPingLF_COCG_Sampler), uv).x);

    // framebufQ2AtrousPongLF_COCG  rg16f
    imageStore(framebufQ2AtrousPongLF_COCG, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2AtrousPongLF_COCG, pix).x);
    v += float(texelFetch(framebufQ2AtrousPongLF_COCG_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2AtrousPongLF_COCG_Sampled, framebufQ2AtrousPongLF_COCG_Sampler), uv).x);

    // framebufQ2AtrousPingHF  r32ui
    imageStore(framebufQ2AtrousPingHF, pix, uvec4(1u));
    v += float(imageLoad(framebufQ2AtrousPingHF, pix).x);
    v += float(texelFetch(framebufQ2AtrousPingHF_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufQ2AtrousPingHF_Sampled, framebufQ2AtrousPingHF_Sampler), uv).x);

    // framebufQ2AtrousPongHF  r32ui
    imageStore(framebufQ2AtrousPongHF, pix, uvec4(1u));
    v += float(imageLoad(framebufQ2AtrousPongHF, pix).x);
    v += float(texelFetch(framebufQ2AtrousPongHF_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufQ2AtrousPongHF_Sampled, framebufQ2AtrousPongHF_Sampler), uv).x);

    // framebufQ2AtrousPingSpec  r32ui
    imageStore(framebufQ2AtrousPingSpec, pix, uvec4(1u));
    v += float(imageLoad(framebufQ2AtrousPingSpec, pix).x);
    v += float(texelFetch(framebufQ2AtrousPingSpec_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufQ2AtrousPingSpec_Sampled, framebufQ2AtrousPingSpec_Sampler), uv).x);

    // framebufQ2AtrousPongSpec  r32ui
    imageStore(framebufQ2AtrousPongSpec, pix, uvec4(1u));
    v += float(imageLoad(framebufQ2AtrousPongSpec, pix).x);
    v += float(texelFetch(framebufQ2AtrousPongSpec_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufQ2AtrousPongSpec_Sampled, framebufQ2AtrousPongSpec_Sampler), uv).x);

    // framebufQ2AtrousPingMoments  rg16f
    imageStore(framebufQ2AtrousPingMoments, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2AtrousPingMoments, pix).x);
    v += float(texelFetch(framebufQ2AtrousPingMoments_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2AtrousPingMoments_Sampled, framebufQ2AtrousPingMoments_Sampler), uv).x);

    // framebufQ2AtrousPongMoments  rg16f
    imageStore(framebufQ2AtrousPongMoments, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2AtrousPongMoments, pix).x);
    v += float(texelFetch(framebufQ2AtrousPongMoments_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2AtrousPongMoments_Sampled, framebufQ2AtrousPongMoments_Sampler), uv).x);

    // framebufQ2GradLFPing  rg16f
    imageStore(framebufQ2GradLFPing, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2GradLFPing, pix).x);
    v += float(texelFetch(framebufQ2GradLFPing_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2GradLFPing_Sampled, framebufQ2GradLFPing_Sampler), uv).x);

    // framebufQ2GradLFPong  rg16f
    imageStore(framebufQ2GradLFPong, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2GradLFPong, pix).x);
    v += float(texelFetch(framebufQ2GradLFPong_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2GradLFPong_Sampled, framebufQ2GradLFPong_Sampler), uv).x);

    // framebufQ2GradHFSpecPing  rg16f
    imageStore(framebufQ2GradHFSpecPing, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2GradHFSpecPing, pix).x);
    v += float(texelFetch(framebufQ2GradHFSpecPing_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2GradHFSpecPing_Sampled, framebufQ2GradHFSpecPing_Sampler), uv).x);

    // framebufQ2GradHFSpecPong  rg16f
    imageStore(framebufQ2GradHFSpecPong, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2GradHFSpecPong, pix).x);
    v += float(texelFetch(framebufQ2GradHFSpecPong_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2GradHFSpecPong_Sampled, framebufQ2GradHFSpecPong_Sampler), uv).x);

    // framebufQ2GradSmplPos  r32ui
    imageStore(framebufQ2GradSmplPos, pix, uvec4(1u));
    v += float(imageLoad(framebufQ2GradSmplPos, pix).x);
    v += float(texelFetch(framebufQ2GradSmplPos_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufQ2GradSmplPos_Sampled, framebufQ2GradSmplPos_Sampler), uv).x);

    // framebufQ2GradSmplPos_Prev  r32ui
    imageStore(framebufQ2GradSmplPos_Prev, pix, uvec4(1u));
    v += float(imageLoad(framebufQ2GradSmplPos_Prev, pix).x);
    v += float(texelFetch(framebufQ2GradSmplPos_Prev_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufQ2GradSmplPos_Prev_Sampled, framebufQ2GradSmplPos_Prev_Sampler), uv).x);

    // framebufQ2Color  rgba16f
    imageStore(framebufQ2Color, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2Color, pix).x);
    v += float(texelFetch(framebufQ2Color_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2Color_Sampled, framebufQ2Color_Sampler), uv).x);

    // framebufQ2TaaOutput  rgba16f
    imageStore(framebufQ2TaaOutput, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2TaaOutput, pix).x);
    v += float(texelFetch(framebufQ2TaaOutput_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2TaaOutput_Sampled, framebufQ2TaaOutput_Sampler), uv).x);

    // framebufQ2TaaHistory  rgba16f
    imageStore(framebufQ2TaaHistory, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2TaaHistory, pix).x);
    v += float(texelFetch(framebufQ2TaaHistory_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2TaaHistory_Sampled, framebufQ2TaaHistory_Sampler), uv).x);

    // framebufQ2TaaHistory_Prev  rgba16f
    imageStore(framebufQ2TaaHistory_Prev, pix, vec4(1.0));
    v += float(imageLoad(framebufQ2TaaHistory_Prev, pix).x);
    v += float(texelFetch(framebufQ2TaaHistory_Prev_Sampled, pix, 0).x);
    v += float(texture(sampler2D(framebufQ2TaaHistory_Prev_Sampled, framebufQ2TaaHistory_Prev_Sampler), uv).x);

    // framebufQ2RngSeed  r32ui
    imageStore(framebufQ2RngSeed, pix, uvec4(1u));
    v += float(imageLoad(framebufQ2RngSeed, pix).x);
    v += float(texelFetch(framebufQ2RngSeed_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufQ2RngSeed_Sampled, framebufQ2RngSeed_Sampler), uv).x);

    // framebufQ2RngSeed_Prev  r32ui
    imageStore(framebufQ2RngSeed_Prev, pix, uvec4(1u));
    v += float(imageLoad(framebufQ2RngSeed_Prev, pix).x);
    v += float(texelFetch(framebufQ2RngSeed_Prev_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufQ2RngSeed_Prev_Sampled, framebufQ2RngSeed_Prev_Sampler), uv).x);

    // framebufQ2Cluster  r32ui
    imageStore(framebufQ2Cluster, pix, uvec4(1u));
    v += float(imageLoad(framebufQ2Cluster, pix).x);
    v += float(texelFetch(framebufQ2Cluster_Sampled, pix, 0).x);
    v += float(texture(usampler2D(framebufQ2Cluster_Sampled, framebufQ2Cluster_Sampler), uv).x);

    probeOutput[0] = v;
}
